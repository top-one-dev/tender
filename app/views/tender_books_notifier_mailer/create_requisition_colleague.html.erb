<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h2>Hi, <%= @colleague.name %></h2>
    <h3>You were added to requisition #<%= @requisition.id %> for <b><%= @requisition.name %></b> by <%= @requisition.contact_name %></h3>
    <p>Check for status updates and communicate with the buyer by clicking on the following link:</p>
    <%= link_to 'Show your requisition', "#{requisition_url(@requisition.id)}?token=#{@token}" %>
    <p>You will be notified when the buyer has processed the requisition.</p>
  </body>
</html>