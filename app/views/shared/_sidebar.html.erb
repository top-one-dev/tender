<div class="panel-group">
  <div class="panel panel-info">
    <div class="panel-heading dropdown">
      <a href="javascript:;" data-toggle="dropdown">
        <% if defined? new_company %>
          Add Company
        <% else %>
          <% if current_company.nil? %>
          Add Company
          <% else %>
          <%= current_company.name %>
          <% end %>
        <% end %> <i class="glyphicon glyphicon-triangle-bottom pull-right"></i></a>
        <ul class="dropdown-menu">
          <% current_user.companies.each do |company| %>
          <li><%= link_to company.name, edit_company_path( company ) %></li>
          <% end %>
          <li class="divider"></li>
          <li>
            <%= link_to 'Add Company', new_company_path, class: 'btn btn-info add-company' %>
          </li>
        </ul>
      </div>
      <div class="panel-body">
        <% if defined? new_company %>
          <%= image_tag 'buyer/noimageCompany.gif', class: 'img-responsive', style: 'margin: 0 auto;' %>
        <% else %>
          <% if current_company.nil? %>
            <%= image_tag 'buyer/noimageCompany.gif', class: 'img-responsive', style: 'margin: 0 auto;' %>
          <% elsif current_company.logo.empty? %>
            <%= image_tag 'buyer/noimageCompany.gif', class: 'img-responsive', style: 'margin: 0 auto;' %>
          <% else %>
            <%= image_tag current_company.logo, class: 'img-responsive', style: 'margin: 0 auto;' %>
          <% end %>
        <% end %>                        
      </div>
    </div>

    <% if defined? new_company %>
      <div class="panel panel-success">
        <div class="panel-heading"><i class="glyphicon glyphicon-triangle-left"></i> Add Company</div>
      </div>
    <% else %>
      <% unless current_company.nil? %>
        <% if defined? company %>
          <div class="panel panel-success">
            <div class="panel-heading">
              <%= link_to '<i class="glyphicon glyphicon-triangle-left"></i> General'.html_safe, edit_company_path(current_company) %>
            </div>
          </div>
        <% else %>
          <div class="panel panel-info">
            <div class="panel-heading">
              <%= link_to '<i class="glyphicon glyphicon-triangle-right"></i> General'.html_safe, edit_company_path(current_company) %>
            </div>
          </div>
        <% end %>

        <% if defined? company_user %>
          <div class="panel panel-success">
            <div class="panel-heading">
              <%= link_to '<i class="glyphicon glyphicon-triangle-left"></i> Company users'.html_safe, company_path(current_company) %>
            </div>
          </div>
        <% else %>
          <div class="panel panel-info">
            <div class="panel-heading">
              <%= link_to '<i class="glyphicon glyphicon-triangle-right"></i> Company users'.html_safe, company_path(current_company) %>
            </div>
          </div>
        <% end %>
        
        <% if defined? subscription %>
          <div class="panel panel-success">
            <div class="panel-heading">
              <%= link_to '<i class="glyphicon glyphicon-triangle-left"></i> Subscription'.html_safe, '#' %>
            </div>
          </div>
        <% else %>
          <div class="panel panel-info">
            <div class="panel-heading">
              <%= link_to '<i class="glyphicon glyphicon-triangle-right"></i> Subscription'.html_safe, '#' %>
            </div>
          </div>
        <% end %>

      <% end %>
    <% end %>

  </div>

  <hr>

  <div class="panel-group">
    <% if defined? user %>
      <div class="panel panel-success">
        <div class="panel-heading">
          <%= link_to '<i class="glyphicon glyphicon-triangle-left"></i> Your profile'.html_safe, edit_user_registration_path %>
        </div>
      </div>
    <% else %>
      <div class="panel panel-info">
        <div class="panel-heading">
          <%= link_to '<i class="glyphicon glyphicon-triangle-right"></i> Your profile'.html_safe, edit_user_registration_path %>
        </div>
      </div>
    <% end %>

    <% if defined? notification %>
      <div class="panel panel-success">
        <div class="panel-heading">
          <% if current_user.notification.nil? %>
          <%= link_to '<i class="glyphicon glyphicon-triangle-left"></i> Notifications'.html_safe, new_notification_path %>
          <% else %>
          <%= link_to '<i class="glyphicon glyphicon-triangle-left"></i> Notifications'.html_safe, edit_notification_path(current_user.notification) %>
          <% end %>
        </div>
      </div>
    <% else %>
      <div class="panel panel-info">
        <div class="panel-heading">
          <% if current_user.notification.nil? %>
          <%= link_to '<i class="glyphicon glyphicon-triangle-right"></i> Notifications'.html_safe, new_notification_path %>
          <% else %>
          <%= link_to '<i class="glyphicon glyphicon-triangle-right"></i> Notifications'.html_safe, edit_notification_path(current_user.notification) %>
          <% end %>      
        </div>
      </div>
    <% end %>
    
  </div>